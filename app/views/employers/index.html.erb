<h1>Employers</h1>
<ul>
	<% @employers.each do |employer| %>
		<li>
			<%= link_to employer.name, employer_path(employer) %> (<%= link_to "edit", edit_employer_path(employer) %>)
		</li>
	<% end %>

	<li><%= link_to "Create New Employer", new_employer_path %></li>
</ul>
